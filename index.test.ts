import { PingIdentityDirectory } from "./index";

describe("ping-identity-directory-typescript-sdk", () => {
    it("initialize client", async () => {
        const pingidentitydirectory = new PingIdentityDirectory({
        });
    });
});
